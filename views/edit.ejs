<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Health Entry</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <div class="page-header">
            <h1>Edit Health Entry</h1>
            <p>Update your health metrics.</p>
        </div>
        <form action="/edit/<%= entry.id %>" method="POST">
            <label for="date">Date:</label>
            <input type="date" id="date" name="date" value="<%= entry.date %>" required>

            <label for="weight">Weight (kg):</label>
            <input type="number" id="weight" name="weight" step="0.1" value="<%= entry.weight %>" required>

            <label for="bloodPressure">Blood Pressure (e.g., 120/80):</label>
            <input type="text" id="bloodPressure" name="bloodPressure" value="<%= entry.bloodPressure %>" required>

            <label for="heartRate">Heart Rate (bpm):</label>
            <input type="number" id="heartRate" name="heartRate" value="<%= entry.heartRate %>" required>

            <label for="steps">Steps:</label>
            <input type="number" id="steps" name="steps" value="<%= entry.steps %>" required>

            <label for="sleepHours">Sleep Hours:</label>
            <input type="number" id="sleepHours" name="sleepHours" step="0.1" value="<%= entry.sleepHours %>" required>

            <label for="spO2">SpO2 (%):</label>
            <input type="number" id="spO2" name="spO2" step="0.1" value="<%= entry.spO2 %>" required>

            <label for="temperature">Temperature (°C):</label>
            <input type="number" id="temperature" name="temperature" step="0.1" value="<%= entry.temperature %>" required>

            <button type="submit" class="btn primary">Update Entry</button>
        </form>
        <a href="/" class="btn secondary">Back to Home</a>
    </div>

    <!-- Bottom Navigation -->
    <nav class="navbar">
        <a href="/" class="nav-item">
            <i class="fas fa-home nav-icon"></i>
            Home
        </a>
        <a href="/monitor" class="nav-item">
            <i class="fas fa-chart-line nav-icon"></i>
            Monitor
        </a>
        <a href="/appointments" class="nav-item">
            <i class="fas fa-calendar-alt nav-icon"></i>
            Appointments
        </a>
        <a href="/profile" class="nav-item">
            <i class="fas fa-user nav-icon"></i>
            Profile
        </a>
    </nav>
</body>
</html>
